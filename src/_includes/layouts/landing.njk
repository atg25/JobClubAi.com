<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <meta name="description" content="AInspire - Where Students Grow Into Future-Ready Professionals">
    <link rel="preload" as="image" href="{{ '/images/BG_new.webp' | baseUrl }}">
    <link rel="stylesheet" href="{{ '/css/main.css' | baseUrl }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Girassol&family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>
      body {
        position: relative;
        background-color: #0A1117;
      }
      
      body::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-position: center;
        background-repeat: no-repeat;
        background-size: cover;
        opacity: 1;
        z-index: -1;
      }
    </style>
</head>
<body class="font-sans antialiased text-slate-50 min-h-screen flex flex-col">
    <script>
      (function() {
        const isDev = window.location.hostname === 'localhost';
        const bgUrl = isDev ? '/images/BG_new.webp' : '/JobClubAi.com/images/BG_new.webp';
        const style = document.createElement('style');
        style.textContent = `body::before { background-image: url('${bgUrl}'); }`;
        document.head.appendChild(style);
      })();
    </script>
    <main class="flex-grow">
        {{ content | safe }}
    </main>
    
    <footer class="py-16 px-6 sm:px-8 lg:px-4 border-t border-white/20 backdrop-blur-md" style="background: linear-gradient(180deg, rgba(255, 255, 255, 0.02) 0%, rgba(255, 255, 255, 0.05) 100%);">
      <div class="container-unified">
        <!-- Footer Logo & Nav -->
        <div class="text-center mb-8">
          <a href="#home" class="inline-block">
            <h2 class="text-white text-4xl mb-8 transition-all duration-300 hover:[text-shadow:0_0_15px_rgba(255,255,255,0.5),0_0_30px_rgba(255,255,255,0.3)] cursor-pointer" style="font-family: 'Girassol', cursive;">AInspire</h2>
          </a>
          <nav class="flex flex-wrap justify-center items-center gap-6 lg:gap-8 text-white/90">
            <a href="#home" class="text-base hover:text-white transition-all duration-300 hover:[text-shadow:0_0_15px_rgba(255,255,255,0.5),0_0_30px_rgba(255,255,255,0.3)]">Home</a>
            <a href="#join" class="text-base hover:text-white transition-all duration-300 hover:[text-shadow:0_0_15px_rgba(255,255,255,0.5),0_0_30px_rgba(255,255,255,0.3)]">Join</a>
            <a href="#events" class="text-base hover:text-white transition-all duration-300 hover:[text-shadow:0_0_15px_rgba(255,255,255,0.5),0_0_30px_rgba(255,255,255,0.3)]">Events</a>
            <a href="#resources" class="text-base hover:text-white transition-all duration-300 hover:[text-shadow:0_0_15px_rgba(255,255,255,0.5),0_0_30px_rgba(255,255,255,0.3)]">Resources</a>
            <a href="#portfolio" class="text-base hover:text-white transition-all duration-300 hover:[text-shadow:0_0_15px_rgba(255,255,255,0.5),0_0_30px_rgba(255,255,255,0.3)]">Portfolio Tips</a>
          </nav>
        </div>
        
        <!-- Divider -->
        <div class="border-t border-white/10 my-8"></div>
        
        <!-- Footer Bottom -->
        <div class="flex flex-col sm:flex-row justify-center items-center gap-4 text-white/70 text-sm">
          <span>Copyright © AInspire</span>
          <span class="hidden sm:inline">•</span>
          <a href="#" class="hover:text-white transition-colors duration-300">Privacy Policy</a>
        </div>
      </div>
    </footer>
</body>
</html>